import 'package:flutter/material.dart';

class FragmentHomeExpenses extends StatelessWidget {
  const FragmentHomeExpenses({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Center(
        child: Text("Fragment Expenses"),
      ),
    );
  }
}
