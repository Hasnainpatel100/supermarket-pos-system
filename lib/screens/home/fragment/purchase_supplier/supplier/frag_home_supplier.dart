import 'package:flutter/material.dart';

class FragHomeSupplier extends StatelessWidget {
  const FragHomeSupplier({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Center(
        child: Text("Fragment supplier"),
      ) ,
    );
  }
}
