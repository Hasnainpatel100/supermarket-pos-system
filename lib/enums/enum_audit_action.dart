/// Enum representing the type of action taken, used in audit log entries.
enum AuditAction {
  create,
  update,
  delete,
  view,
  login,
  logout,
  print,
  export,
  import,
  cancel,
  approve,
  reject,
  enable,
  disable,
}
